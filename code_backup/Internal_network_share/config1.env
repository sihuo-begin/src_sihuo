

# 基本设置
HOST=10.200.147.81
PORT=8000
#UPLOAD_DIR=uploads
UPLOAD_DIR=C:\Pthon_Dir\PMI_Test_Tranining
MAX_UPLOAD_MB=200

# 要浏览/查看的根目录（改成你的共享路径）
ROOT_DIR=C:\Pthon_Dir\PMI_Test_Tranining          # Windows 映射盘
# ROOT_DIR=/mnt/share/projects  # Linux 挂载点
``


# 访问认证
AUTH_USERNAME=admin
AUTH_PASSWORD=123456

# 邮件通知（Office 365 / 企业邮 / QQ/163 邮均可）
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USE_TLS=true
SMTP_USER=your_account@yourdomain.com
SMTP_PASS=your_smtp_password_or_app_password
SMTP_FROM=your_account@yourdomain.com
SMTP_TO=alice@yourdomain.com,bob@yourdomain.com
# 上传是否把文件作为附件一起发（大文件不建议）
EMAIL_ATTACH_FILE=false
``

``
